# ── LLM (optional — stub mode works without these) ──
OPENAI_API_KEY=
OPENAI_BASE_URL=
LMQ_LLM_MODEL=gpt-4o-mini

# ── Azure Cloud (optional — local-first by default) ──
AZURE_STORAGE_ACCOUNT=
AZURE_KEYVAULT_URL=
MLFLOW_TRACKING_URI=

# ── Azure Identity (for local dev against Azure resources) ──
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
